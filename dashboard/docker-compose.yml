services:
  nova-shop-dashboard:
    container_name: nova-shop-dashboard
    restart: always
    ports:
      - 3000:3000
    networks:
      - nova_shop_network

networks:
  nova_shop_network:
    external: false
